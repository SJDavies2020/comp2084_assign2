@model Assign1.Models.Cmake

@{
    ViewData["Title"] = "Details";
}

<h1>Details</h1>

<div>
    <h4>Cmake</h4>
    <hr />
    <dl class="row">
        <dt class = "col-sm-2">
            @Html.DisplayNameFor(model => model.CarMake)
        </dt>
        <dd class = "col-sm-10">
            @Html.DisplayFor(model => model.CarMake)
        </dd>
    </dl>
</div>
<div>
    @if (User.Identity.IsAuthenticated)
    {
        <a asp-action="Edit" asp-route-id="@Model.CmakeId">Edit</a>
    }
       <a asp-action="Index">Back to List</a>
</div>
